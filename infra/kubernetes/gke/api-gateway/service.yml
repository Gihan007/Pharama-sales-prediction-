apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: drug-sales
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  type: ClusterIP
  selector:
    app: api-gateway
  ports:
    - name: http
      port: 5000
      targetPort: 5000
