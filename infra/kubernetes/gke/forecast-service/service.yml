apiVersion: v1
kind: Service
metadata:
  name: forecast-service
  namespace: drug-sales
spec:
  type: ClusterIP
  selector:
    app: forecast-service
  ports:
    - name: http
      port: 5001
      targetPort: 5001
