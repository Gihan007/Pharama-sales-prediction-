apiVersion: v1
kind: Service
metadata:
  name: training-service
  namespace: drug-sales
spec:
  type: ClusterIP
  selector:
    app: training-service
  ports:
    - name: http
      port: 5002
      targetPort: 5002
